# Vercel deployment configuration
server.port=${PORT:8080}

# FreeSQLDatabase MySQL configuration
spring.datasource.url=${DATABASE_URL:jdbc:mysql://sql12.freesqldatabase.com:3306/sql12796422}
spring.datasource.username=${DATABASE_USERNAME:sql12796422}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Logging configuration
logging.level.net.javaguides=INFO
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO
