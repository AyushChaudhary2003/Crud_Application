# Example environment variables for production deployment
# Copy this file to .env and update with your values

# Docker Hub username (required for production deployment)
DOCKER_USERNAME=your-docker-username

# MySQL Configuration
MYSQL_ROOT_PASSWORD=your-secure-password
MYSQL_PASSWORD=your-ems-password

# Frontend API URL (use relative path for same-domain deployment)
REACT_APP_API_URL=/api/employees
